%h1 Listing news_items
%table
  %thead
    %tr
      %th Title
      %th Body
      %th
      %th
      %th
  %tbody
    - @news_items.each do |news_item|
      %tr
        %td= news_item.title
        %td= news_item.body
        %td= link_to 'Show', news_item
        %td= link_to 'Edit', edit_news_item_path(news_item)
        %td= link_to 'Destroy', news_item, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New News item', new_news_item_path
